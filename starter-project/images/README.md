# images

Put your own picture in this folder (for example `hero.jpg`), then update the `src` in `index.html` to point to it.

`example.png` is the reference screenshot shown in the main `README.md` — it's not part of your project, leave it as is.
